<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto" >

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name=".Sts2Application"
        android:label="@string/godot_project_name_string"
        android:allowBackup="false"
        android:icon="@mipmap/icon"
        android:appCategory="game"
        android:isGame="true"
        android:hasFragileUserData="false"
        android:requestLegacyExternalStorage="false"
        tools:ignore="GoogleAppIndexingWarning" >
        <profileable
            android:shell="true"
            android:enabled="true"
            tools:targetApi="29" />

        <activity
            android:name=".GameSettingsActivity"
            android:label="@string/godot_project_name_string"
            android:theme="@style/Theme.Sts2ExtraSettings"
            android:icon="@mipmap/icon"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true">

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/settings_shortcuts" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GodotApp"
            android:label="@string/godot_project_name_string"
            android:theme="@style/GodotAppSplashTheme"
            android:launchMode="singleInstancePerTask"
            android:excludeFromRecents="false"
            android:exported="true"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="layoutDirection|locale|orientation|keyboardHidden|screenSize|smallestScreenSize|density|keyboard|navigation|screenLayout|uiMode"
            android:resizeableActivity="false"
            tools:ignore="UnusedAttribute" >

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/game_shortcuts" />

        </activity>

        <activity
            android:name=".Sts2CrashActivity"
            android:label="@string/sts2_crash_activity_name"
            android:theme="@style/Theme.Sts2Crash"
            android:exported="false"
            android:process=":error_activity"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="cat.ereza.customactivityoncrash.ERROR" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NexusModsStoreActivity"
            android:label="@string/nexus_mod_store_title"
            android:theme="@style/Theme.Sts2ExtraSettings"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true" />

        <activity
            android:name=".LogViewerActivity"
            android:label="@string/log_viewer_title"
            android:theme="@style/Theme.Sts2Crash.AppBar"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true" />

        <activity
            android:name=".LogFileViewerActivity"
            android:label="@string/log_file_viewer_title"
            android:theme="@style/Theme.Sts2Crash.AppBar"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true" />

        <activity
            android:name=".FileBrowserActivity"
            android:label="@string/file_browser_title"
            android:theme="@style/Theme.Sts2Crash.AppBar"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true" />

        <activity
            android:name=".TextEditorActivity"
            android:label="@string/text_editor_title"
            android:theme="@style/Theme.Sts2Crash.AppBar"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"
                tools:replace="android:resource" />
        </provider>

    </application>

</manifest>
